<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- TODO: Use <include> and <exclude> tags to control what is backed up.
        <include .../>
        <exclude .../>
        -->
    </cloud-backup>
    <!-- Device transfers (e.g. Android Auto or Android TV) -->
    <device-transfer>
        <!-- TODO: Use <include> and <exclude> tags to control what is transferred.
        <include .../>
        <exclude .../>
        -->
    </device-transfer>
</data-extraction-rules>